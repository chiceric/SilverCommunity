<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="silver">
  
  <!-- 로그인 select -->
  <select id="login" 
  resultType="silver.web.vo.LoginVO"
  parameterType="java.util.Map">
  	SELECT * FROM silver_t
  	WHERE s_tel=#{s_tel} AND s_pwd=#{s_pwd}
  </select>
  
  <!-- 회원가입 insert -->
  <insert id="add" 
  parameterType="silver.web.vo.LoginVO">
  	INSERT INTO silver_t(s_tel, s_name, s_birth, s_addr, s_gender, s_status,
	s_date, s_pwd, s_idx, s_nickname)
  	VALUES(#{s_tel},#{s_name},#{s_addr},#{s_birth}, #{s_gender},
  	1, sysdate, #{s_pwd}, seq_silver_idx.NEXTVAL, #{s_nickname})
  </insert>
</mapper>
